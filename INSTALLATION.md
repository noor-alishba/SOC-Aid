# Installation

## Requirements

- Python 3.x
- Google Colab
- Groq API key
- Internet connection
- Required Python packages

## Install Dependencies

```python
!pip install -q langchain langchain-groq langgraph pandas
```

## API Key

Add the following secret to Google Colab:

`GROQ_API_KEY`

The API key must never be committed to the GitHub repository.
